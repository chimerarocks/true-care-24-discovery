{
  "name": "true-care-24-discovery",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "sass": "^1.26.11",
    "stylelint": "^13.7.1",
    "stylelint-config-property-sort-order-smacss": "^6.3.0",
    "stylelint-config-sass-guidelines": "^7.1.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-styles": "docker run -u 1000 --rm -v $(pwd):/node -w /node node ./node_modules/.bin/sass css/style.scss css/style.css",
    "build": "docker run -u 1000 --rm -v $(pwd):/node -w /node node npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
